version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
    # Enforce a 7-day minimum release age before creating PRs
    cooldown:
      default-days: 7

  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
    # Enforce a 7-day minimum release age before creating PRs
    cooldown:
      default-days: 7
    # Group all npm dependencies into a single PR
    groups:
      all-dependencies:
        patterns:
          - "*"
