version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
    # Enforce a 5-day minimum release age before creating PRs
    cooldown:
      default-days: 5
    # Group all npm dependencies into a single PR
    groups:
      all-dependencies:
        patterns:
          - "*"
